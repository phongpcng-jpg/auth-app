package com.example.authapp.entity;

/**
 * Application roles. Only USER is active today; more roles (e.g. ADMIN)
 * can be added later without changing the column type (stored as STRING).
 */
public enum Role {
    USER
}
